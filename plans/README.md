# Implementation Plans

Generated by the improve skill on 2026-06-23 against commit `e850768`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, run every verification
gate, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Remove destructive migration, export Room schema | P1 | M | — | DONE |
| 002  | Characterization tests for status/carry-over core | P1 | M | — | DONE |
| 003  | DayProgressBar day-aware rendering | P1 | M | — | DONE |
| 004  | Editing preserves in-progress event state | P1 | M | 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 004 should land after 002: 002 establishes the repository-level safety net
  (status/carry-over tests) so the ViewModel behavior change in 004 can't
  silently regress the fragile status logic. 004 also adds its own focused
  ViewModel regression test.
- 001, 002, 003 are mutually independent and may be executed in parallel by
  different executors (they touch disjoint files).

## Verification baseline

The repo has no CI. Before starting any plan, confirm the baseline builds:

```
./gradlew testDebugUnitTest
```

If this does not pass on a clean checkout, STOP and report — the plans assume a
green baseline. (Android SDK must be configured via `local.properties` /
`ANDROID_HOME`; the wrapper `./gradlew` is present.)

## Findings considered and rejected

- F4 (dead `cal2` in `shiftToTargetDay`): trivial dead code; not worth a
  dedicated plan. Will be cleaned up incidentally if 002 touches the same
  function — otherwise leave it.
- F9 (magic `1` in EventDao SQL): low-value churn; revisit when a second status
  query is added.
- F10 (empty `Converters` class): dead code, harmless; leave for now.
- F11 (duplicated `dayRange` logic): minor DRY; not worth the diff risk yet.
- F12 (SDK 34 → 35/36): medium-risk bump with no blocking need for a local app;
  defer.
- F13 (CI/lint/minify): valuable but out of scope for this batch; batch separately.
- F14 (no ProGuard/release signing): defer with F13.
- F15 (stale AGENT.md structure): docs cleanup; do alongside the next feature
  work, not as a standalone plan.
- F6 (`allowBackup="true"`): low-severity privacy; bundle into a future
  hardening pass.
- F7 (unbounded `getAllNotes`): matters at scale; revisit when heatmap (V2) lands.
- Direction items D1–D4: not planned in this batch.
