# 事记本（ShiJiBen）设计文档

> 日期：2026-06-22
> 状态：已评审

## 一、产品定位

### 是什么

一个纯粹的本地时间记录工具。受《奇特的一生》启发，核心理念是：**单纯地记录生活，不设 TODO、不强迫复盘，让记录本身成为习惯。** 通过长期记录，自然看见时间和精力花在哪里，不对自己评判，减少内耗。

### 不是什么

- 不是日历应用、不是 TODO 应用、不是复盘应用
- 不是计划清单、不是时间管理、不是任务管理、不是打卡应用
- 不联网、无社区、无提醒推送

### 核心哲学

1. **记录即审视**：未经审视的人生不值得一过。记录是审视的前提。如果每做两件事之间都要犹豫、墨迹、准备很久，就会形成空白。目的是减少空白。
2. **App 反映现实，不评判现实**：事情没做就是没做，这个事实应该可见。预写的事件如果没做，顺延到明天——不是 nagging，是诚实。不让一件事彻底消失在意识之外。
3. **轻量存在**：时间可视化静静在那，不制造焦虑。打开 App 感受到的是平静和掌控感，不是压力。

## 二、技术架构

### 技术选型

- **平台**：Android 原生应用
- **语言**：Kotlin
- **UI**：Jetpack Compose
- **架构**：MVVM + Repository 模式
- **本地存储**：Room（SQLite 抽象层）
- **依赖注入**：Hilt
- **异步**：Kotlin Coroutines + Flow
- **导航**：Jetpack Navigation Compose

### 项目结构

```
shijiben/
  docs/            # 文档：设计 spec、需求记录
  design/          # 设计思考、HTML 原型/mockup
  app/
    src/main/java/com/shijiben/
      core/        # 主题、8-bit 调色板、共享组件
      data/        # Room 数据库、Entity、DAO、Repository
      features/
        timeline/  # 时间轴主视图
        recording/ # 记录功能
        tags/      # 标签管理
        heatmap/   # 热力图（v2）
        time_viz/  # 时间可视化（v2）
    src/main/res/  # Android 资源
    build.gradle.kts
    AndroidManifest.xml
  build.gradle.kts
  settings.gradle.kts
```

### 设计原则

- 数据模型简单，不过度抽象。Event 和 Note 是独立表，不强行合并。
- 8-bit 美学集中在 `core/theme`，不散落各处。
- 本地优先：所有数据在设备上，不依赖任何网络。

## 三、数据模型

### Event（事件）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long PK | 自增 |
| title | String | 做了什么 |
| startTime | Long | 开始时间戳（毫秒） |
| endTime | Long? | 结束时间戳，null = 进行中 |
| status | Int | `0=not_started` / `1=in_progress` / `2=completed` |
| tagId | Long? FK | 关联标签 |
| note | String? | 内联备注（可选） |
| createdAt | Long | |
| updatedAt | Long | |

### Note（随笔）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long PK | 自增 |
| content | String | 文字内容 |
| timestamp | Long | 记录时刻 |
| createdAt | Long | |
| updatedAt | Long | |

### Tag（标签）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long PK | 自增 |
| name | String | 标签名 |
| color | Int | 8-bit ARGB 色值 |
| sortOrder | Int | 排序 |
| createdAt | Long | |
| updatedAt | Long | |

### 事件状态流转

```
预写 ──→ not_started
             │
     开始计时/手动标记
             │
             ▼
        in_progress（end_time = null）
             │
         停止计时
             │
             ▼
         completed（填入 end_time）
```

**自动顺延机制**：

- App 打开时检查，把之前日期的 `not_started` 事件移到今天（start_time 改为次日相同时刻）
- 不做后台定时（符合本地、不联网原则）
- 目的：不让预写的事情彻底消失在意识之外

## 四、核心 UI：时间轴

### 布局

- **左侧时间条**：上下两个正方形色块
  - 第一个色块：0:00 - 12:00
  - 第二个色块：12:00 - 24:00
  - 蓝色 = 已过去未记录，绿色 = 有事件记录，米白 = 还未到
  - 当前小时有红色边框标记
- **右侧事件列表**：像素卡片展示，标题 + 时间 + 时长 + 状态
- **底部"记一笔"按钮**：统一记录入口
- **当前时间指示**： subtly 标示现在

### 8-bit 美学

- **色板**：活泼高饱和 NES 风格（鲜红、翠绿、亮紫、桃红、金黄、靛蓝等）
- **组件**：像素边框、硬阴影、复古字体
- **集中管理**：`core/theme` 中定义色板常量、像素边框组件、字体配置

## 五、记录流程

所有记录通过底部"记一笔"按钮进入。

### 新增事件弹窗

- 标题输入框
- 时间范围滑块：
  - 0-12 小时一行
  - 12-24 小时一行
  - 开始、结束两个端点可拖动
  - 默认范围 30 分钟
- 标签选择（横向色块 chips）
- 确定按钮

### 事件规则

- start_time >= now → `not_started`（预写）
- start_time < now 且 end_time <= now → `completed`（补录）
- 进行中的事件通过单独"开始计时"入口创建

## 六、标签系统

- **首次打开**：空标签起步，用户自行创建
- **标签管理**：用户可增删改标签，为每个标签选择 8-bit 色板中的颜色
- **作用**：分类与着色。在时间轴上通过颜色一眼区分事件类型
- **不做**：不做时长汇总统计（v1），不做层级标签

## 七、回看与可视化（V2）

### GitHub 式热力图

- 每一天是一个方块
- **填充 = 记录的时间覆盖比例**：没有记录 = 空白方块；记录了一半时间 = 面积填一半
- 价值：照见"空白"。未记录的时间 = 未审视的时间 = 可能的犹豫和墨迹
- 目的：减少空白，告诉自己"我知道我在做什么，你看，都记录了"

### 时间可视化（轻量存在）

- **今天剩余时间**：打开 App 时静静可见（如"今天还剩 6h 23m"）
- **今年/一生剩余时间**：在单独页面，想看才看
- 不制造焦虑，只是提醒时间在流逝

### 随笔功能（V2 完整化）

- V1 仅支持时间轴右侧点的展示
- V2 补充：随笔的创建入口、列表查看、编辑删除

## 八、分期计划

### V1：灵魂闭环

- 时间轴主视图（含日期切换）
- 记录功能：底部"记一笔"按钮 + 时间范围滑块弹窗
- 事件状态流转 + 自动顺延
- 标签管理（空起步，增删改着色）
- 8-bit 活泼复古主题
- 随笔：时间轴右侧点展示 + 基础创建

### V2：回看与凝视

- GitHub 式热力图（覆盖比例填充）
- 今天/今年/一生剩余时间可视化
- 随笔功能完整化（列表、编辑、删除）

### V3：打磨

- 搜索
- 数据导出/备份
- 其他根据使用反馈迭代

## 九、待定/开放问题

- 8-bit 活泼色板的具体色值：在 `design/` 中出 HTML mockup 后确定
- 时间滑块默认范围 30 分钟是否合适：需要原型验证手感
- 自动顺延的具体时刻处理（相同时刻 vs 重新安排）：v1 先用相同时刻，根据使用反馈调整
